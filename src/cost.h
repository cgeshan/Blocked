// Pouya
#ifndef COST_H_IS_INCLUDED
#define COST_H_IS_INCLUDED

#include <vector>

#include "Block.h"

float CalculateCost(std::vector <Block> layer);
// Takes one array, represented as a vector of blocks, and returns 

#endif